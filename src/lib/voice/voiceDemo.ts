/**
 * Hermes Voice UI / realtime session surface.
 * Reactive implementation lives in `voiceSession.svelte.ts` (Svelte 5 runes).
 */
export {
	createVoiceDemo,
	type VoiceDemo,
	type VoiceDemoState
} from './voiceSession.svelte.js';
