/** Server facade — deep-imports mint (never via client-safe barrels). */
export { mintRealtimeClientSecret } from '$lib/providers/xai/mint.server';
export type { EphemeralClientSecret } from '$lib/providers/types';
